"""Node catalog — all Onyx workflow node implementations.

Organized by extension:
  - onyx_nodes.py    — Onyx utility, file, voice, agent nodes
  - evera_nodes.py   — EVERA music generation (thin wrappers over connector)
  - smartengine_nodes.py — SmartEngine writing (thin wrappers over connector)
  - justedit_nodes.py — JustEdit video editing (thin wrappers over connector)
"""
